# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'EchoZero Dashboards'
    orgId: 1
    folder: 'EchoZero'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true
